# GeoTech SpareFinder - Environment Variables Template
# Copy this file to .env and update with your actual values

# =============================================
# REQUIRED - Application will not work without these
# =============================================

# Supabase Configuration (REQUIRED)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# AI Service Configuration (REQUIRED)
VITE_AI_SERVICE_URL=http://localhost:8000
VITE_AI_SERVICE_API_KEY=your-ai-service-api-key

# Application Configuration (REQUIRED)
VITE_APP_URL=http://localhost:5173
VITE_APP_NAME="GeoTech SpareFinder"

# =============================================
# IMPORTANT - Needed for production features
# =============================================

# Authentication & Security
VITE_JWT_SECRET=your-jwt-secret-key-min-32-chars
VITE_ENABLE_OAUTH=true
VITE_OAUTH_GOOGLE_CLIENT_ID=your-google-client-id
VITE_OAUTH_GITHUB_CLIENT_ID=your-github-client-id

# Stripe Payment Processing
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Email Configuration (for analysis completion notifications)
VITE_EMAIL_SERVICE=smtp
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com
SMTP_SECURE=false
FRONTEND_URL=http://localhost:5173

# =============================================
# OPTIONAL - Can be left empty for basic functionality  
# =============================================

# Analytics & Monitoring
VITE_ANALYTICS_ID=your-analytics-id
VITE_SENTRY_DSN=your-sentry-dsn
VITE_MIXPANEL_TOKEN=your-mixpanel-token

# File Storage (if using external storage)
VITE_CLOUDINARY_CLOUD_NAME=your-cloudinary-name
VITE_CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-secret

# AWS S3 (alternative file storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket-name

# Security & CORS
VITE_ALLOWED_ORIGINS=http://localhost:5173,https://your-domain.com
VITE_CORS_ENABLED=true
VITE_RATE_LIMIT_MAX=100
VITE_RATE_LIMIT_WINDOW=15

# Redis (for caching and sessions)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=your-redis-password

# API Configuration
VITE_API_TIMEOUT=30000
VITE_MAX_FILE_SIZE=10485760
VITE_SUPPORTED_FORMATS=jpg,jpeg,png,webp

# Development & Debug
VITE_DEBUG=false
VITE_LOG_LEVEL=info
NODE_ENV=development

# =============================================
# NOTES
# =============================================
# 1. All VITE_ prefixed variables are available in the frontend
# 2. Variables without VITE_ prefix are backend/server-side only
# 3. Never commit .env files to version control
# 4. Use different values for development, staging, and production
# 5. Keep sensitive values secure and rotate them regularly 