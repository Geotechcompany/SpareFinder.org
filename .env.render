# SpareFinder AI - Render Environment Variables Template
# Copy these to your Render dashboard when deploying

# ===========================================
# BACKEND ENVIRONMENT VARIABLES (Node.js)
# ===========================================

NODE_ENV=production
PORT=10000

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# Authentication
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long

# Frontend URL (your Netlify deployment)
FRONTEND_URL=https://your-netlify-app.netlify.app

# AI Service URL (will be available after AI service deployment)
AI_SERVICE_URL=https://sparefinder-ai-service.onrender.com

# CORS Origins
ALLOWED_ORIGINS=https://your-netlify-app.netlify.app,http://localhost:5173

# ===========================================
# AI SERVICE ENVIRONMENT VARIABLES (Python)
# ===========================================

PORT=10000
PYTHONUNBUFFERED=1
ENVIRONMENT=production

# Supabase Configuration (same as backend)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# AI Service Authentication
API_KEY=your_ai_service_api_key_here

# Google Search API (optional but recommended)
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_SEARCH_ENGINE_ID=your_google_custom_search_engine_id

# ===========================================
# FRONTEND ENVIRONMENT VARIABLES (Netlify)
# ===========================================

# Update these in your Netlify dashboard after backend deployment
VITE_API_URL=https://sparefinder-backend.onrender.com
VITE_AI_SERVICE_URL=https://sparefinder-ai-service.onrender.com
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ===========================================
# DEPLOYMENT ORDER
# ===========================================

# 1. Deploy Backend first (get the URL)
# 2. Deploy AI Service second (get the URL)
# 3. Update Backend AI_SERVICE_URL with AI service URL
# 4. Update Frontend environment variables with both URLs
# 5. Redeploy Frontend

# ===========================================
# IMPORTANT NOTES
# ===========================================

# - Replace all "your_*" placeholders with actual values
# - Keep service role keys secure and never commit to git
# - Test each service health endpoint after deployment
# - Monitor Render logs for any deployment issues
# - Free tier services sleep after 15 minutes of inactivity 