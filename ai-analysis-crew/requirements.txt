fastapi==0.115.0
uvicorn[standard]==0.30.3
python-multipart==0.0.6

# websockets is pulled by realtime/uvicorn and (via stagehand) google-genai.
# Keep it compatible with google-genai (<15.1,>=13) and realtime (<16,>=11).
websockets==14.2

setuptools>=65.5.0
crewai==0.80.0
crewai-tools==0.14.0
langchain-openai==0.2.14
# Pin OpenAI client to avoid resolver backtracking during Docker builds on Render
# NOTE: stagehand requires openai>=1.99.6
openai==1.99.6
reportlab==4.0.7
pillow==10.1.0
python-dotenv==1.0.0
tiktoken>=0.7.0
requests>=2.31.0

# Storage and database integration
# NOTE: Older supabase versions pin httpx<0.25 which conflicts with modern AI libs (anthropic/stagehand).
supabase==2.25.1
postgrest==2.25.1

# Shared HTTP client version to keep resolver deterministic (must satisfy supabase/openai/anthropic/stagehand)
httpx==0.28.1

# CrewAI transitive deps (pin to keep Render builds deterministic / fast)
litellm==1.74.15
stagehand==0.5.6
anthropic==0.52.1
google-genai==1.55.0

