fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Pin websockets to 11.x because websockets 12 removed the `websockets.asyncio`
# module that `supabase-py` still imports, which causes:
#   ImportError: No module named 'websockets.asyncio'
websockets==11.0.3

setuptools>=65.5.0
crewai==0.80.0
langchain-openai
# Pin heavy AI dependencies to avoid long dependency resolution during builds.
# crewai 0.80.0 requires openai>=1.13.3 and langchain-openai 0.2.0 requires
# openai>=1.40.0,<2.0.0, so we pick a single compatible version.
openai
litellm
stagehand==0.5.5
langsmith==0.4.52
reportlab==4.0.7
pillow==10.1.0
python-dotenv==1.0.0
tiktoken>=0.7.0
requests>=2.31.0

# Storage and database integration
supabase==2.3.0
postgrest>=0.13.0

