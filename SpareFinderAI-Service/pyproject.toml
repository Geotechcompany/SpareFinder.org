[build-system]
requires = ["setuptools>=61.0", "wheel", "pillow"]
build-backend = "setuptools.build_meta"

[project]
name = "SpareFinderAI-Service"
version = "1.0.0"
description = "AI-powered automotive part identification service"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["app"] 